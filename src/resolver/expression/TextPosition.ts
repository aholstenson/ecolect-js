/**
 * Position within a text.
 */
export interface TextPosition {
	/**
	 * Start position, zero indexed and inclusive.
	 */
	start: number;

	/**
	 * End position, zero indexed and exclusive.
	 */
	end: number;
}
